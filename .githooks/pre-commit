#!/usr/bin/env bash
set -euo pipefail

npm run format:check
npm run lint
npm run typecheck

if command -v gitleaks >/dev/null 2>&1; then
  gitleaks protect --staged --redact
else
  printf '%s\n' 'gitleaks is not installed; skipping secret scan.'
fi
